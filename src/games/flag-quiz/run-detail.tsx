/*
 * The run page rows for a shared Flag Quiz run (blueprint 7.7). The saved resultJson is the
 * frozen submission shape { score, total, answers }: option indices, not countries, so there
 * is nothing to name honestly here. GenericDetail prints the two numbers it does carry.
 */
export { GenericDetail as RunDetail } from "@/games/_shared/generic-detail";
