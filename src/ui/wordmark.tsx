import Link from "next/link";

/** `Countrivo` in Erode 600 22 px, no tracking, 44 px tall hit area (blueprint 3.1). */
export function Wordmark() {
  return (
    <Link href="/" className="wm t-wm">
      Countrivo
    </Link>
  );
}
